<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalProject">
        <activity
            android:name=".UpdateName"
            android:label="Редактирование"/>
        <activity
            android:name=".ChangeImage"
            android:label="Редактирование"
            android:theme="@style/Theme.FinalProject" />
        <activity
            android:name=".Profile"
            android:label="Личный кабинет"
            android:theme="@style/Theme.FinalProject" />
        <activity
            android:name=".AddExpenActivity"
            android:label="Добавление расходов" />
        <activity
            android:name=".AddProfitActivity"
            android:label="Добавление доходов" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.FinalProject.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="Авторизация">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity"
            android:label="Регистрация" />
        <activity
            android:name=".RegisterActivity2"
            android:label="Регистрация" />

    </application>

</manifest>